# 2026-02-02 (Asia/Saigon)

## Session summary
- User asked to review repo for issues
- Found and fixed multiple bugs

## Bugs fixed today

### 1. `pick_sr_zone` function broken (P0)
- **File:** `trading/cron_15m_context.py`
- **Issue:** Function body was empty — logic code was placed AFTER `wma()` function (unreachable)
- **Fix:** Moved filter logic back into `pick_sr_zone`, now returns proper S/R zones

### 2. Close logic too aggressive (P0)
- **File:** `trading/cron_15m_bot.py`
- **Issue:** Auto-closed positions when RSI entered 40-60 (violated clarified rule from 2026-01-31)
- **Fix:** RSI 40-60 now emits WARNING only, HOLD position. Close only when bias flips.

### 3. CSV schema mismatch (P1)
- **File:** `trading/apply_15m_decision.py`
- **Issue:** Different column schema than `cron_15m_bot.py` (missing `bias1h`, `bias4h`, `bias1d`)
- **Fix:** Aligned schemas, added missing args and columns

### 4. Workspace setup incomplete
- Created `IDENTITY.md` (name=Clawd, emoji=🦁)
- Updated `USER.md` with Trong's info
- Created this daily memory file

## BTC Analysis (16:00 UTC)
- Price: ~$78,829
- HTF (1D/4H): Bearish (RSI 1D=29, 4H=39) — chưa < 20 nên chưa "mạnh thực sự"
- LTF (15m/30m): Bullish (RSI ~62-64) — chưa > 80 nên chưa "mạnh thực sự"
- **Conflict/fueling regime** — no MTF confluence
- Recommendation: WAIT

## Rule corrections (từ user feedback)

### 1. RSI extreme (không phải 60/40)
- RSI > 60 is NOT "strong", only > 80 is truly strong (overbought)
- Updated SKILL.md: added Rule C (extreme 80/20)
- RSI zones: >80 strong | 60-80 bullish | 40-60 balance | 20-40 bearish | <20 strong

### 2. Phân tích lực = xem history
- "Quá máy móc" — chỉ lấy 1 điểm không đủ
- Phải xem 120-240 candles để thấy quán tính
- Đọc flow: RSI đang đi như thế nào? Peaks/troughs đang cao hay thấp dần?

### 3. LLM-first approach
- Không code Python để tính stats (avg, %, distribution)
- Chỉ fetch raw numbers → LLM tự đọc và phân tích
- Code chỉ để lấy data, không để ra quyết định

### 4. Regime từ pattern
- Bull range: RSI 40-80, hồi giữ > 40
- Bear range: RSI 20-60, rally kẹt < 60
- Xem nhiều peaks/troughs để xác định, không chỉ 1 điểm

### 5. Kỷ luật: Đã vào lệnh = chờ (Feb 3)
- **Không open/close liên tục** - đã vào thì commit
- 3h không move ≠ trade sai → chỉ là chưa đến lúc
- RSI hồi về balance ≠ invalidation → chờ SL hoặc TP
- Chỉ close khi: (1) Hit SL hoặc (2) Hit TP
- **Không close vì:** hoang mang, không chắc, "không move"

## Files modified
- `trading/cron_15m_context.py`
- `trading/cron_15m_bot.py`
- `trading/apply_15m_decision.py`
- `IDENTITY.md` (created)
- `USER.md` (updated)
- `memory/2026-02-02.md` (created)
